<Window x:Class="BPMLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BPM Launcher" Height="300" Width="450" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="BanPickMod Launcher" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="SC2 Path: " FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock x:Name="TxtSc2Path" Text="Not Found" VerticalAlignment="Center" Margin="5,0"/>
            <Button Content="Browse..." Width="70" Click="BtnBrowse_Click" Margin="5,0"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,10">
            <TextBlock x:Name="TxtModStatus" Text="Mod File: Checking..." />
            <TextBlock x:Name="TxtMapStatus" Text="Map File: Checking..." />
        </StackPanel>

        <Button x:Name="BtnRunTestMap" Grid.Row="4" Content="로컬 테스트 맵 실행" Height="40" FontSize="14" FontWeight="Bold" Click="BtnRunTestMap_Click" IsEnabled="False"/>
    </Grid>
</Window>
